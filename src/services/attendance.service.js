// DI SINI logic panjang kamu berada
export async function handleCheckIn({ user, lat, lng, file }) {
  // 1. Validasi
  // 2. Ambil lokasi
  // 3. Hitung jarak
  // 4. Insert attendance
  // 5. Upload selfie
  // 6. Update URL
  // 7. Return result
}
